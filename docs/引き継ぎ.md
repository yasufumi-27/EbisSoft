# 引き継ぎ・進捗管理（Handoff & Progress）

> このファイルは **他セッション／他担当（AI含む）への引き継ぎ用** です。
> 作業を始める前に、まずこのファイルと `README.md`・`docs/仕様書.md` を読んでください。
> 最終更新：2026-07-30

---

## 0. このプロジェクトは何か

- **エビスソフト**（京都市伏見区）の、**Web制作の受注（集客）を目的としたサービスサイト**。
- **一旦のゴール**：このサイトを**公開（本番リリース）**すること。
- **コンセプト**：「**AIを駆使して、最速で、高性能なサイトを**」＋ **SEO / AEO / LLMO（AI検索最適化）に超特化**。
- **差別化の核**：「できること」を主張で終わらせず、**15領域すべてを実際に動くデモとして公開**している
  （3DCG / AR / 商品カスタマイズ / 料金シミュレーター / AIレコメンド / 行動解析・A/Bテスト /
  アニメーション / AIチャットボット / 音声AI / 多言語 / AIエージェント対応 / パーソナライズ / PWA / SNS連携 / システム連携）。
- **将来構想**：AIで受託業務を遂行する「**Web制作エージェント**」の構築（→ 6章。今は構想のみ）。

---

## 1. 新セッションが最初に知るべきこと（重要・つまずき防止）

1. **これは“いつものNext.js”ではない**：**Next.js 16**（App Router / Turbopack）。APIが学習データと異なる場合がある。
   コードを書く前に `node_modules/next/dist/docs/` の該当ガイドを読む（`AGENTS.md` の指示）。
2. **Bashサンドボックスがネットワークを遮断している**：`npm install` / `npm run build`（`next/font`のフォント取得）/ 外部取得は
   **`dangerouslyDisableSandbox: true` を付けて実行**する。付けないと `ENOTFOUND registry.npmjs.org` で失敗する。
3. **シェルは zsh**：`shopt` 等のbash builtinは使えない。必要なら `bash -c '...'` で実行する。
4. **単一情報源（ここだけ直せば全体に反映）**：
   - `src/lib/site.ts` … 社名・URL・NAP・SNS・keywords・knowsAbout
   - `src/lib/content.ts` … 掲載内容（サービス/料金/実績/お客様の声/FAQ/要点/AEO）
   - `src/lib/jsonld.ts` … 構造化データ（JSON-LD）ビルダー
   - `src/app/globals.css` … ブランドカラー・フォント（`@theme`）
5. **`★` 印 = 差し替え必須のプレースホルダ**（`site.ts` 等）。
6. **検証コマンド**（下記5章）でビルド・型・Lint・実機HTMLを必ず確認する。
7. **技術メモ**：Tailwind **v4**（`@theme`／CSS変数）。`"use server"` ファイルは**async関数しかexport不可**
   （型・定数は別ファイルへ。例：`src/app/contact-state.ts`）。OG画像のsatori既定フォントは**日本語非対応**（画像内はラテン文字主体）。

---

## 2. 現在の進捗（✅ 完了）

**土台＋トップページを、SEO/AEO/LLMO実装込みで構築・ビルド検証済み。**

- ✅ プロジェクト土台：Next.js 16 / React 19 / TS / Tailwind v4
- ✅ トップページ（1枚）：Hero → 要点 → 強み → サービス → AI検索対策 → 実績 → 料金 → 流れ → お客様の声 → FAQ → お問い合わせ
- ✅ 共通：ヘッダー（モバイルメニュー）／フッター（NAP）
- ✅ お問い合わせフォーム：Server Action＋バリデーション＋ハニーポット（**※送信処理は未実装＝TODO**）
- ✅ SEO：メタ／canonical／OGP・Twitter／sitemap／robots／manifest／各アイコン／セキュリティヘッダ／CWV配慮
- ✅ **AEO**：構造化データ（`ProfessionalService`/`WebSite`/`WebPage`/`BreadcrumbList`/`ItemList`/`HowTo`/`FAQPage`）＋`Speakable`＋結論ファースト要点ブロック
- ✅ **LLMO**：`/llms.txt` 設置／AIクローラー（GPTBot・ClaudeBot・PerplexityBot・Google-Extended 等）を明示歓迎／`knowsAbout`
- ✅ ドキュメント：`README.md` / `docs/仕様書.md` / `docs/ヒアリングシート.md`
- ✅ ビルド：12ルート静的生成・`tsc`・`lint` すべてクリーン
- ✅ **デザイン全面リニューアル（2026-07-17）**：近未来×高級感のダークテーマへ刷新
  - テーマ：漆黒ネイビー基調＋シアン（brand）／バイオレット（accent）／シャンパンゴールド（gold）。トークンは `globals.css` の `@theme`
  - **3D背景**：`three` を導入。`src/components/fx/ThreeBackground.tsx`（パーティクル星野＋ワイヤーフレーム多面体＋床グリッド、マウスパララックス、タブ非表示で停止、reduced-motion対応）。`BackgroundFx.tsx` が `next/dynamic`（ssr:false）で遅延読込するため初期HTML/LCPに影響なし
  - **スクロールリビール**：`fx/RevealInit.tsx`＋`[data-reveal]`（CSSはglobals.css）。JS無効時はnoscriptで打ち消し、SEO/AEOへの影響なし
  - UI：コーナーカット＆シーン掃引のボタン（`.btn`系）、ガラスパネル＋ゴールドコーナー（`.panel`系）、発光フォーム（`.field`）、Orbitron（`--font-display`）を英字見出し・数値に使用
  - OG画像・apple-icon・icon.svg・manifestの配色も新テーマへ同期済み
  - 検証済み：`tsc`/`lint`/`build` クリーン、ヘッドレスChromeでPC・モバイル表示とコンソールエラーなしを確認
- ✅ **GitHub Pages公開（2026-07-17）**：https://yasufumi-27.github.io/EbisSoft/
  - `GITHUB_PAGES=true` で `output:"export"`＋`basePath:/EbisSoft`（`next.config.ts`）。mainへのpushで自動デプロイ（`.github/workflows/deploy-pages.yml`）
  - フォームは mailto 方式（静的書き出しではServer Actions不可。実送信実装時は `actions.ts` に戻す）
- ✅ **デザイン強化第2弾＋E-E-A-T＋サービス拡充（2026-07-17）**
  - 演出追加：カードのマウス追従スポットライト（`PointerFx`）、統計カウントアップ（`CountUp`）、ヒーローの3D Tilt（`Tilt`）、スクロール進捗バー（`ScrollProgress`）、テックマーキー（`TechMarquee`）、フィルムノイズ質感、3D背景のスクロールパララックス
  - **E-E-A-T**：代表プロフィール＋品質基準セクション（`Expert.tsx`、`#quality`）を新設。`content.ts` の `expert` / `qualityStandards`（★要差し替え）と `personJsonLd()`（Person構造化データ、organization.founder / webPage.author・reviewedBy から参照）
  - **サービス拡充**：「3DCG制作・WebGL演出」「動画・モーショングラフィックス制作」を services に追加（JSON-LD・llms.txtへ自動反映）。keywords / knowsAbout / FAQ / keyFacts / ヒーロー文言 / メタdescription にも反映

- ✅ **AI訴求へ全面刷新＋実動デモ実装（2026-07-29）**
  - **社名を `EbisSoft` → `EbisuSoft` に変更**、拠点を東京・恵比寿 → **京都市伏見区**へ（`site.ts` / NAP / geo / areaServed / localAreas）
  - **削除**：制作実績（Works）・お客様の声（Testimonials）・「誰が、どんな基準で作るか」（Expert）の3セクション。
    あわせて `personJsonLd` と `aggregateRating`（架空のレビュー評価）も構造化データから撤去
  - **新設セクション**：`AiPower`（AI活用の工程別 AI/人 分担・Before→Afterの短縮実績）、
    `Capabilities`（できること一覧＋その場で動くデモをタブ切替）
  - **実動デモ5種**（`src/components/demos/`）：
    - `Demo3dcg` … Three.js のリアルタイムWebGL（形状/素材/カラー/光量/自動回転/OrbitControls、環境マップで金属・ガラス表現）
    - `DemoAnimation` … CSS + Web Animations API のみで8パターン（外部ライブラリ不使用）
    - `DemoChatbot` … `src/lib/kb.ts` の**日本語N-gram + BM25検索**で根拠つき回答。閾値未満は答えず問い合わせへ誘導
    - `DemoSns` … SNSフィード（サンプルデータ）＋ OGPカードプレビュー（入力に即時追従する実装）
    - `DemoIntegration` … ブラウザ内モックAPI。遅延・失敗・指数バックオフのリトライ・Webhook通知が実際に動く
    - すべて `DemoLoader`（`next/dynamic` ssr:false）で遅延読込 → 初期表示・LCPに影響なし
  - **新規ページ**：`/demo`（一覧）、`/demo/[slug]`（5ページ・静的生成）、`/company`（会社概要）、`/privacy`
  - **SEO/E-E-A-T**：構造化データ刷新（`hasOfferCatalog` / `geo` / `Service`+`WebApplication` / `CollectionPage` / `AboutPage`）、
    表示パンくず（`Breadcrumbs`）、sitemap 9URL、llms.txt をデモ・料金・AI活用まで拡充
  - **バグ修正**：`RevealInit` のリビール状態を class → `data-revealed` 属性へ変更。
    className を動的に切り替える要素が再レンダリングされると React が class を書き戻して**要素が消える**不具合があった
  - **basePath対応の修正**：`manifest.webmanifest` のアイコン／`start_url` と、動的生成ルートである
    `apple-icon` に basePath が自動付与されず404していたため、`NEXT_PUBLIC_BASE_PATH`（`next.config.ts` の `env`）で明示付与するよう修正
  - ⚠️ **既知の挙動（GitHub Pages限定）**：`trailingSlash: false` のため `/demo/` のように
    末尾スラッシュ付きでアクセスすると404になる（`/demo` は200）。サイト内リンク・sitemap・llms.txt は
    すべてスラッシュなしで統一しているため実害はないが、独自ドメイン移行時はホスティング側でリダイレクト設定を推奨
  - 検証済み：`tsc` / `lint` / `build`（通常・`GITHUB_PAGES=true` の両方）クリーン、
    ヘッドレスChromeでデモの実動作30項目・PC/モバイル表示・横はみ出しゼロ・JSエラーゼロを確認

- ✅ **導入効果の訴求・情報整理・問い合わせ導線の刷新（2026-07-30）**
  - **できること**：各capabilityに `impact`（事業インパクト1行）・`businessValue`（導入で何が変わるか3点）・
    `buildTime`（AI活用での実装時間。5つ合計で約1時間＝10〜15分/件）を追加。カードは技術名ではなく「事業がどう変わるか」を主役に変更
  - **文字量の整理**：strengths / services / aiWorkflow の説明文を短縮し、長い注記は `<details>`（`.demo-note`）へ移動。
    本文はDOMに残しているのでSEO/AEOへの影響なし
  - **お問い合わせを専用ページ化**：`/contact` を新設し、トップは遷移ボタンのみに。
    選択項目は `src/lib/inquiry.ts`（予算・維持費・ページ数・端末・ブラウザ・フォーム要否・機能・時期・目的の9グループ）。
    必須はお名前・メール・「やりたいこと」の3つだけにして離脱を防ぐ設計。旧 `ContactForm.tsx` は削除
  - **不具合修正**：`RevealInit` が「12%見えたら表示」だったため、画面より背の高い要素（長いフォーム）が
    いつまでも表示されず空白になっていた。画面より高い要素は接触時点で表示するよう変更

- ✅ **トレンド機能を全面実装／デモを11領域へ拡張（2026-07-30）**
  - **新デモ6種**：`ar`（WebXRで実物大表示／非対応環境は実寸プレビューへ自動切替）、
    `voice`（Web Speech APIで実際に聞き取り・読み上げ）、`multilingual`（Intl APIで通貨・日付をロケール変換＋hreflang生成）、
    `ai-agent`（**このサイトの実 llms.txt を fetch し、実 JSON-LD を解析**）、`personalize`（`?seg=` で自動判定＋差分表示）、
    `pwa`（Service Workerの登録状況・キャッシュ数・通知許可を**実測表示**）
  - **チャットボットに予約完結フロー**：会話内で日程→時間→確定まで進み、受付番号を発行（`BOOKING_RE` で意図検出）
  - **View Transitions**：`next.config.ts` の `experimental.viewTransition` ＋ `layout.tsx` の `<ViewTransition>`。
    CSSは `::view-transition-old/new(root)`。reduced-motion では無効化
  - **PWA**：`public/sw.js`（HTMLはネットワーク優先、静的アセットはstale-while-revalidate、pushハンドラ実装済み）＋
    `fx/PwaInit.tsx` で登録。basePath付きスコープに対応
  - **アクセシビリティ**：`:focus-visible` のフォーカスリング、`main` へのフォーカス移動
    （※スキップリンクは 2026-08-02 にご要望で削除）
  - **iOS / macOS 対応（2026-07-30 追記）**：
    - AR は3方式を自動で出し分け。(1) WebXR（Android Chrome等）(2) **カメラ重ね合わせ**（`getUserMedia` +
      `DeviceOrientationEvent`。iOS は `requestPermission()` が必要）(3) 実寸プレビュー。
      Macなどジャイロがない環境はドラッグで見回せる
    - 音声AIは Safari 14.1+ が `webkitSpeechRecognition` に対応しているため元から動作する。
      ただし iOS/macOS Safari は**ユーザー操作の中で一度 speak() を呼ぶまで読み上げが鳴らない**ため、
      ボタン押下時に無音を1回流して解錠している（`unlockSpeech`）。cancel直後のspeak無音対策で60ms待つ
    - ⚠️ **重要な修正**：`next.config.ts` の `Permissions-Policy` が `camera=(), microphone=()` で
      自サイトのカメラ・マイクを禁止していた。`camera=(self), microphone=(self)` に修正。
      静的配信（GitHub Pages）ではヘッダーが出ないため気づきにくいが、通常ホスティングでは
      ARのカメラと音声AIのマイクが動かなくなる
  - **デモ選択時の自動スクロール**：カード一覧の下にデモがあるため、選んだだけでは画面外のままだった。
    `showDemo()` で `#live-demo` へ `scrollIntoView`（reduced-motion では即時ジャンプ）
  - 合計時間は実測ベースで按分し直し：**11個で約2時間**（3DCG15/AR15/チャット20/他10分）
  - サービス・FAQ・お問い合わせの機能選択肢にも新領域を反映
  - 検証：デモ実動作 31/31＋既存30/30、フォーム、スキップリンク、View Transitions、
    PC/モバイル表示（横はみ出し0・JSエラー0）を確認

- ✅ **売上に直結する機能を4種追加／デモを15領域へ拡張＋デザインの洗練（2026-07-30）**
  - **新デモ4種**（いずれも「集客・購買欲求」に直結する領域。計算・描画はすべて本物）：
    - `simulator`（料金シミュレーター・自動診断）… 種類/ページ数/機能/デザイン/SEOから
      概算金額・内訳・期間（従来比）・おすすめプラン・月額を即時算出。結果をコピーして問い合わせへ引き継げる
    - `configurator`（商品カスタマイズ）… SVGをその場で組み立てて描画。サイズ/張地/カラー/脚/オプションで
      見た目・価格・納期・構成コードがリアルタイムに変わる（在庫と閲覧者数のみサンプル値）
    - `recommend`（AIレコメンド）… 特徴ベクトルのコサイン類似度（コンテンツベース75%）＋
      サンプルセッションから作った共起行列（協調フィルタリング25%）で並べ替え。推薦理由とスコア内訳も表示
    - `insight`（行動解析・A/Bテスト）… クリック位置を実記録し Canvas にヒートマップ描画（パレットLUT方式）。
      A/Bテストは訪問者シミュレーション＋2標本Z検定で有意差判定（サンプル不足時は「判定できない」と出す）
  - 合計時間を実測で按分し直し：**15個で約3時間**（`stats` / `strengths` / `keyFacts` / llms.txt / /demo も同期）
  - サービスに「CVR改善・行動解析・A/Bテスト」「EC・商品カスタマイズ・レコメンド」を追加、FAQ4問追加、
    お問い合わせの機能選択肢に4項目追加
  - **デザインの洗練（IT／AIらしさの強化）**：
    - ヒーローの右パネルを、意味のないダッシュボードのモックから
      **AI制作パイプラインのコンソール**（`sections/HeroConsole.tsx`）へ変更。工程ごとの AI／人 の分担が順に走る
    - `fx/CursorGlow.tsx`（カーソル追従の光。rAFで間引き。タッチ／reduced-motionでは非表示）
    - `fx/SectionNav.tsx`（右端のHUDドットナビ。IntersectionObserverで現在位置。1280px以上で表示）
    - `.aurora`（ごくゆっくり流れるアンビエント光）／`.panel` に上辺のハイライト（ガラスの厚み）
    - **CSSスクロール駆動アニメーション**（`animation-timeline: view()`）で見出し下のラインが引かれる（JS不使用）
    - ヒーロー見出しは行ごとに立ち上げる（`.hero-line`。文字をspan分割しないので折り返し・読み上げに影響なし）
    - 日本語組版：`text-wrap: balance/pretty`、h1に `word-break: keep-all`（語の途中で改行されない）
    - 金額の表示は Orbitron（0にスラッシュが入り読みづらい）をやめ、本文フォント＋`tabular-nums` に
  - ⚠️ **バグ修正（重要）**：`ThreeBackground` が `new THREE.WebGLRenderer()` の例外を捕まえておらず、
    **WebGLが使えない環境（GPU無効・古い端末・一部の企業ポリシー）でページ全体が真っ白になっていた**。
    try/catch で静かに諦めるよう修正（背景は装飾なので他は正常に動く）
  - 検証：`tsc`/`lint`/`build`（通常・`GITHUB_PAGES=true`）クリーン、ヘッドレスChromeで
    新デモの実動作（価格・構成コード・SVGの色・推薦順・ヒートマップ描画・有意差判定）を確認、
    PC/モバイル（390px）で横はみ出しゼロ・JSエラーゼロ

- ✅ **日本語の組版（語中改行）を全ページで修正／見出し文言の刷新（2026-08-02）**
  - **問題**：ブラウザは日本語をどの文字間でも折り返すため、「チャット／ボット」「シミュレー／ター」のように
    カタカナ語が途中で割れていた（実測：8ページ × 3画面幅で 87 箇所）。
  - **対策1（組版）**：`src/lib/typography.tsx` の `ja(text)` が、カタカナの連なりと
    記号入り英数字（Wi-Fi / N-gram / llms.txt）を `<span class="nb">` `<span class="nb-strict">` で包む。
    CSSは `globals.css`（`.nb` は `word-break: keep-all` ＋ 収まらないときだけ `overflow-wrap: break-word`、
    `.nb-strict` は `display:inline-block; white-space:nowrap`）。
    **文字は一切足していない**（U+2060 等を挟んでいない）ので、コピー・読み上げ・SEO・JSON-LD に影響しない。
    見出しなど JSX を受け取る共通部品では `jaNode()`（文字列だけ処理）を使う。
  - **対策2（Chromeのみ）**：`@supports (word-break: auto-phrase)` で文節折り返しを有効化。
    「つくり／ます」のような切れ方が減る。Safari / Firefox は未対応だが、対策1で語割れは防げている。
  - ⚠️ **落とし穴**：折り返し禁止の語を含む行は、Chromeが**flex項目の最小幅（min-content）を過大に見積もる**。
    その結果カードがグリッドのトラック幅を超え、320px幅で横スクロールが出た。
    → `.panel` に `min-width: 0`、アイコン＋テキストの `li` のテキスト側に `min-w-0` を付けて解消。
  - **文言**：AIが書いたような「〜を、〜。」の反復（できること15件のタグライン・強み・各H1・
    セクション見出し）を、具体的で言い切らない表現へ変更。下層ページのH1は
    「よくあるご質問」「基本情報」のように、内容がそのまま分かる名詞句に寄せた（SEOにも有利）。
  - 検証：`tsc` / `lint` / `build:pages` クリーン。ヘッドレスChromeで
    **12ページ × 390/768/1280px の語中改行ゼロ**（`Range#getClientRects` で行頭文字を判定するスクリプトで実測）、
    **8ページ × 320/360/375/390px の横はみ出しゼロ**。

- ✅ **「ご依頼・ご相談」ページの新設／ページ内メニューの追加（2026-08-02）**
  - グローバルナビの **「料金」→「ご依頼・ご相談」（`/request`）** に変更。
    新ページに「相談できること（6分類）」「こんな状態でも大丈夫です」「料金プラン」「料金の考え方」
    「ご相談から着手までの流れ」「料金のFAQ」を集約した。
    データは `content.ts` の `consultTopics` / `consultCases` / `pricingNotes` / `requestSteps` / `pageSummaries.request`。
  - **料金は `/web` から `/request` へ移動**（`<Pricing />` は `/request` のみ）。
    `/web#pricing`・`/#pricing` を指していたリンク（PricingTeaser・デモ詳細・料金シミュレーター・kb.ts）は
    すべて `/request#pricing` に付け替え済み。sitemap・llms.txt・pageLinks も更新。
  - **ページ内メニュー `src/components/site/PageNav.tsx`**：ヘッダー（`h-16`）の直下に `sticky top-16` で貼り付き、
    スクロール中も常に見える。現在地は IntersectionObserver で判定（`data-current`）、狭い画面では横スクロールし、
    現在地が画面外に出ないよう `scrollLeft` を寄せる。トップ・`/ai`・`/web`・`/embedded`・`/request` に設置。
    - ⚠️ `IntersectionObserver` の `rootMargin` は **px か % のみ**。`rem` を渡すと例外になり、
      クライアントコンポーネントのハイドレーションごと落ちる（実際に一度踏んだ）。
    - アンカー位置のズレは `main:has(.pagenav) [id] { scroll-margin-top: 8.5rem }` で吸収。
    - 右端のドットナビ（`fx/SectionNav`）はトップから外した（同じ役割が二重になるため）。`SectionNav` 自体は残置。
  - ヘッダーのデスクトップ表示を **md → lg** に変更（768〜1023pxでナビが折り返して窮屈だったため、その帯はハンバーガー）。
  - **スキップリンク（本文へスキップ）を削除**（ご要望）。`layout.tsx` の `<a class="skip-link">` と
    `globals.css` の `.skip-link` を撤去。復活させる場合は両方を戻すこと。

- ✅ **サイト内AIアシスタント（右下のドット絵キャラクター）を新設（2026-08-02）**
  - 全ページの右下にドット絵キャラクターが常駐し、クリックすると
    「サイトに書いてあることを何でも聞ける」チャットが開く。**デモ（`/demo/ai-chatbot`）とは別の常設機能**
    （デモは検索スコアの可視化と予約フローつきの技術ショーケース。こちらは案内役）。
  - ファイル：`src/components/assistant/`
    - `SiteAssistant.tsx` … 起動役。`layout.tsx` に1行置くだけ
    - `AssistantPanel.tsx` … チャット本体（会話・出典リンク・問い合わせ誘導）
    - `PixelMascot.tsx` … ドット絵（16×16）。画像を使わず、ドットの並びを文字列で持ってSVGの矩形に展開
      （横に連続する同色は1本にまとめるので `<rect>` は80個。拡大しても滲まない・追加の取得なし）
    - CSSは `globals.css` の「サイト内AIアシスタント」ブロック（起動ボタン・吹き出し・パネル・まばたき）
  - ⚠️ **表示速度の鉄則を守るための構造**：`kb.ts` は `content.ts` を読むため、そのまま常駐させると
    1800行ぶんが全ページの初期JSに載る。そこで**起動役は kb を import せず**、
    クリックされて初めて `AssistantPanel` を `next/dynamic`（ssr:false）で読み込む。
    初期JSに増えるのは起動役とドット絵だけ（トップの合計 300KB / JS 197KB。目安の220KB以内）。
  - **誤答の抑制を強化（`kb.ts`）**：BM25のスコアだけで判定していたため、
    「明日の東京の天気は？」のような無関係な質問でも「〜ますか」「でき」等の bi-gram が積み上がり、
    閾値（3.2）を超えて見当違いの回答をしていた（実測 3.72）。対策は2つ。
    1. `focus`（スコアのうち**内容語**＝出現文書が全体の25%以下の語が占める割合）を導入し、
       40%未満なら回答しない。「猫を飼っていますか？」（38%）「ドローンの操縦できますか？」（31%）を弾ける。
    2. `CONFIDENCE_THRESHOLD` を **3.2 → 6.0**。想定質問18問の実測最低スコアが 7.3 だったのに対し、
       まぐれ当たりは 3.7 前後だったため、その間に置いた。
    判定は `isConfident(hit)` に集約し、アシスタントとデモの両方が使う。デモの検索パネルにも「内容語 xx%」を表示。
  - kb.ts の出典リンクがサイト再編前のアンカー（`/#faq`・`/#services` 等）のままで
    飛べなくなっていたので実ページへ修正し、「サイトの案内（ページ一覧）」の知識を追加した。
  - 検証：`tsc` / `lint` / `build:pages` クリーン。ヘッドレスChromeで
    PC(1280)・スマホ(390) × 「右下に常駐／初期状態ではパネル未読込／クリックで開く／回答が返る／
    出典リンクが出る／知識外は答えない／Escで閉じる／横はみ出し0／JSエラー0」＝**22項目すべてPASS**、
    下層4ページでの常駐も確認。

- ✅ **名刺記載の事業内容をサイトへ掲載（2026-08-03）**
  - 単一情報源：`content.ts` の **`businessLines`（8項目）**。`category` が `"embedded"` / `"web"` のどちらかで掲載先が決まる。
    **事業内容の追記・修正はこの配列だけ**を直せば、下記すべてに反映される。
  - 表示：`src/components/sections/BusinessLines.tsx`（`category` 省略で全件）
    - `/company` … 概要テーブルの「事業内容」欄（8項目を `／` 連結）＋ 全件のカード一覧（`#business`）
    - `/embedded` … 「主な事業内容」6項目（`#business`。ページ内メニューにも追加）
    - `/web` … 「主な事業内容」2項目（`#business`。同上）
  - `llms.txt` に「## 事業内容（正式な事業内容）」を追加し、`kb.ts`（チャットボット知識源）にも8件を投入済み。
  - 名刺の表記は**そのまま正とする**（例：「ターゲット用組み込みソフト作成」「BSW・各種RTOSポーティング開発」）。
    名刺の `1/O` は `I/O` の表記ゆれ、`EOS` は **電子発注システム（Electronic Ordering System）** の意（依頼主に確認済み）。
  - 検証：`tsc` / `lint` / `build:pages` クリーン。

- ✅ **ロゴをリニューアル（2026-08-03）**
  - 旧ロゴ（青の3D文字「EBISU」＋金の楕円リング＋赤い「Soft」／グラデーション・影あり）は古い印象のため廃止。
    コンセプトは「**20年以上の信頼を土台に、AI時代へ進化するIT企業**」。
    **「AI企業」ではなく「信頼できる技術会社がAIを活用している」**という見せ方にする
    （＝脳・回路・ロボットなどAIを直接表すモチーフは使わない）。
  - **ロゴ＝シンボルマーク＋ワードマーク**：`EB` の角丸スクエアマーク ＋ `EBISU`（白）＋ `SOFT`（シアン）。
    フラット・3D/グラデーション/影なし。シンボルは E 一文字ではなく **EBISU の綴りに由来する EB**（依頼主の指定）。
    ⚠️ ワードマークだけにするとヘッダーで**ページの見出し文字と区別がつかなくなる**ため、マークは必ず伴わせること。
    マークはインラインSVG（`app/icon.svg` と同じ図形）。`src/components/site/Logo.tsx`
  - **カラー**：メイン＝ネイビー `#0f2e5f`（`globals.css` に `--color-navy` / `--color-navy-light` を追加）／
    アクセント＝シアン `#22d3ee`（既存の `--color-brand`）
  - **サイズ別の出し分け**（16pxで5文字は潰れるため）：
    - 16〜32px … `src/app/icon.svg`・`src/app/favicon.ico` は **`EB` の2文字**。Eの中央バーだけシアンで、極小でもブランドカラーが残る
    - 180px … `src/app/apple-icon.tsx` は `EBISU` / `SOFT` の2段
    - OG画像 … `src/app/opengraph-image.tsx` のロゴをフラットなワードマークに差し替え（旧：グラデーションの「E」タイル）
    - `manifest.ts` の `theme_color` を `#22d3ee` → `#0f2e5f`（ネイビー）へ
  - ⚠️ **favicon.ico の作り直し手順**（変換ツールがこの環境にないため）：
    `icon.svg` を `<img width=256>` で包んだHTMLをヘッドレスChromeで `--screenshot` → Pillow の
    `Image.save(..., sizes=[(16,16),(32,32),(48,48)])` で `.ico` 化。次に変更するときも同じ手順で。
  - **表記ルールを更新**：本文・見出し・メタ・構造化データは従来どおりカタカナの「エビスソフト」のみ。
    **英字を使ってよいのはロゴだけ**（`EBISU SOFT`）。`CLAUDE.md`・`docs/仕様書.md`・`site.ts` のコメントに明記済み
  - 検証：`tsc` / `lint` / `build:pages` クリーン。ヘッドレスChromeでPC/タブレット幅のヘッダー表示、
    16px相当のファビコン可読性、OG画像・apple-icon の生成結果を確認。

- ✅ **依頼主提供の会社ロゴ画像を掲載（透過処理＋3D演出への反映）（2026-08-03）**
  - **透過処理**：元画像は青いグラデーション背景つきのJPEG。背景が滑らかなので、
    **3次多項式で背景色 B(x,y) を推定 → 色距離をアルファに変換 → 背景色を差し引いて元の色を復元**した。
    これでリングのグロー（半透明）が自然に残り、白い紙の上でも青い縁が出ない。
    元画像に描かれていた薄い網目・点・きらめきは、**縮小マスク上の連結成分でロゴ本体とつながる領域だけを残して**除去。
    処理スクリプトは使い捨て（scratchpad）。再処理が要るときは同じ手順（Pillow + NumPy）で再現できる。
  - **書き出し**：`public/logo/` に3種。`ebisu-soft-logo.webp`（976×781・表示用）／
    `ebisu-soft-logo.png`（640×512・WebP非対応環境の控え）／`ebisu-soft-logo-512.webp`（3Dテクスチャ兼、小さく出すとき用）。
    PNGは同じ絵で1.3MBになるためWebPを主にしている。
  - **部品**：`src/components/site/CompanyLogo.tsx`（`<picture>` で WebP→PNG、512w/976w を `sizes` で出し分け）。
    静的書き出しでは next/image の最適化が効かないため、あえて素の `<img>`（寸法固定でCLSなし）。
  - **掲載場所（2026-08-03 夜に修正）**：**ヘッダー／フッターのロゴのシンボルをこの画像に置き換えた**（`Logo.tsx`）。
    右のワードマーク `EBISU SOFT`（白＋シアン）はそのまま残している。画像は44〜56pxでは文字が読めず
    シンボル（青い輪の塊）としてしか働かないため、社名を伝える役目は文字側が持つ。
    ヘッダー用に 176w / 352w の小さい版を追加し、暗いヘッダーで沈まないよう明度を1.22倍にしてある。
    - 最初は `/company` に「会社ロゴ」セクションを作り、フッターにも大きく貼ったが、
      **依頼主のご要望で両方とも撤去**（欲しかったのはヘッダーの置き換えだった）。
    - ⚠️ **ファビコン（`app/icon.svg`・`favicon.ico`）は引き続き `EB` の2文字マーク**。
      16〜32pxではこの画像は完全に潰れるため、置き換えないこと。
  - **サイト背景の3D**（`fx/ThreeBackground.tsx`）：ロゴを貼った板を画面左奥に浮かべ、
    首振り＋上下の漂い＋スクロール連動で動かす。本文の可読性を守るため不透明度は0.3前後、
    位置も `x=-12.5` とコンテンツ幅（max-w-6xl）の外に置いている。読み込み失敗は握りつぶす（装飾のため）。
  - **3DCGデモ**（`demos/Demo3dcg.tsx`）：形状に **「会社ロゴ」** を追加（一覧の先頭）。
    テクスチャは選択されたときに初めて読み込む（初期表示を軽く保つ）。
    ロゴは画像なので素材・カラーは適用せず、選択中は両コントロールを無効化して注記を出す（`ChipButton` に `disabled` を追加）。
    光量・自動回転・視点操作は通常どおり効く。
  - 検証：`tsc` / `lint` / `build:pages` クリーン。ヘッドレスChrome（CDP）で
    PC 1440 / モバイル 390 の `/`・`/company`・`/demo/3dcg` を確認。
    3DCGデモは実際に「会社ロゴ」を押してWebGL上に描画されること、横はみ出しゼロ・JSエラーゼロを確認。

- ✅ **ロゴを「3Dモデル」に作り直し、それを正とした（2026-08-03 夜・第2版）**
  - **経緯**：依頼主提供の原画は光跡・回路模様・浮遊する立方体が多すぎた。
    「文字はそのままで装飾だけ減らす」を画像処理で試したが、**原画は `U` の上をリボンが横切っていて
    文字だけを取り出せない**（＝原画の時点で U が隠れている）。そこで方針を変更し、
    **3Dモデルにできる要素だけ**（EBISU / リング / Soft / 光）に絞って作り直した。
  - **正＝3Dモデル**：`src/components/fx/logo3d.ts`
    - `letters` … 押し出した立体文字 EBISU（素材・カラーを差し替え可能）
    - `soft` … Soft。**固定**（回さない）
    - `ring` … EBISU の周りを回るリング。**文字とは別オブジェクト**（2本＋軌道上の粒）
    - `sparks` … 周りを飛ぶ小さな光（90点。毎フレーム位置を更新）
  - **文字の作り方**：three.js には日本語・英字のフォントが入っていないため、
    サイトが使っている **Geist（SIL OFL 1.1）を wght=900 で固定 → typeface JSON へ変換**した
    `src/components/fx/logoFont.json`（**EBISUoft の8字だけ**なので約2.8KB）。
    変換は fontTools（`instancer` で可変フォントを固定 → `DecomposingRecordingPen` で輪郭を取得）。
    ⚠️ 原画のフォントとは字形が異なる。原画の字形に戻したい場合は、原画から輪郭を起こす必要がある。
  - **ヘッダー／フッターだけ2D（2026-08-03 ご指定）**：サイトタイトルとフッターのロゴは
    `src/components/site/CompanyLogo.tsx` の**インラインSVG（平面）**に変更。立体表現をやめ、
    リング上の5語（CLOUD/SECURITY/NETWORKS/AI/GLOBAL）も削除。文字は `logoFont.json` の輪郭を
    SVGパス化してあるので3Dモデルと同じ字形。**背景の3D演出と3DCGデモは従来どおり3Dのまま**。
    書き出し静止画（下記）はサイト内では未使用になったが、名刺・資料用に残してある。
  - **ヘッダーの静止画はモデルの書き出し**（現在サイト内では未使用）：ヘッドレスChrome（CDP）で
    `scratchpad/render/` の書き出し用ページを 1600×1200・透過で描画 → `toDataURL` → 切り抜き → 4サイズのWebP。
    `public/logo/ebisu-soft-logo-3d-{176,352,512,976}.webp`（176w版は約10KB）＋ PNGの控え。
    **ヘッダーと背景とデモが同じ形**になるのが利点。作り直すときも同じ手順で再現できる。
  - **リングの角度（2026-08-03 最終・ご指定）**：**X軸に対して右上がり45度／Y軸に対して奥へ45度**。
    ⚠️ **1つのオブジェクトに `rotation.x` と `rotation.z` を両方入れても効かない**。
    three の既定オイラー順は XYZ（＝Zから適用）なので、`rotation.z` は「面内の回転」になり
    円環では見た目が変わらない。**「右上がり」用の親グループ（`ringRoll.rotation.z`）と
    「手前へ倒す」子（`ring.rotation.x`）に分ける**こと。これを1度踏んでいる。
    原画のリングに乗っていた **CLOUD / SECURITY / NETWORKS / AI / GLOBAL の5語も3Dで復活**させ、
    帯（半透明のリボン）の上に並べてリングと一緒に回している。
    ⚠️ 帯に完全に寝かせると真横から見ることになり読めないため、各語を `rotateX(-0.8)` だけ手前に起こしてある。
    全体も `group.rotation.x = -0.18` だけ前傾させ、楕円が見えるようにした。
    リングの向こう側に来た語は裏返って（鏡文字に）見えるので、組み立て後に world 法線の向きを見て
    `rotateY(π)` で表を向け直している。
  - **枠は正方形**（ご要望）。書き出しは 176/352/512/976 px の正方形（`CompanyLogo` の width/height も 1:1）。
  - **3DCGデモ**：形状「会社ロゴ」を選ぶと、青のメタル・正面・カメラ自動回転オフに切り替わる
    （リング自体が回るため、カメラも回すと読みづらい）。素材・カラーは文字に効く。
  - **サイト背景**：同じモデルを画面のやや左（`x=-4`）に薄く（不透明度0.62）浮かべる。
    **リングだけ4倍**（`createLogo3d({ ringScale: 4 })`）にして、星（四角い粒）と同じくらいの距離まで広げてある。
    文字が見切れないよう中央寄りに置いている。
    背景シーンには環境マップがないため、**金属マテリアルだと真っ黒に沈む**。弱く自己発光する非金属を渡している。
  - 旧ロゴ画像（`ebisu-soft-logo.webp` 等）は削除。ファイル名が変わるので古いキャッシュとは衝突しないが、
    念のため `public/sw.js` の `VERSION` を v1 → v2 に上げてある。
  - 検証：`tsc` / `lint` / `build:pages` クリーン。ヘッドレスChromeで PC1440・モバイル390 の
    ヘッダー表示、背景の見え方、デモの実描画（60FPS / 15,272 TRI）、横はみ出しゼロ・JSエラーゼロを確認。

> 連絡先・住所は反映済み。ドメイン・SNS・郵便番号・geo座標は引き続き**仮の値（`★`）**（→ 4章）。

---

## 3. サイト構成（2026-08-02 にLP型 → 会社サイト型へ再編）

トップは**要約と入口だけ**。詳細は下層ページに分割し、内部リンクで回遊させる。
遷移してほしい優先度＝グローバルナビの並び順：

| 優先 | パス | 役割 |
| --- | --- | --- |
| 1 | `/ai` | AI活用（AIを使う側＝開発プロセス／作る側＝AI機能の受託、AEO・LLMO） |
| 2 | `/web` | Web制作（AI開発プロセス・サービス・制作の流れ・料金） |
| 3 | `/embedded` | 組み込み開発（ファームウェア／IoT連携／お断りする範囲も明記） |
| 4 | `/demo`・`/demo/<slug>` | できること（実動デモ15種） |
| 5 | `/request` | ご依頼・ご相談（相談できること／**料金**／相談から着手までの流れ） |
| - | `/faq`・`/company`・`/contact`・`/privacy` | 補助 |

- **料金は `/request` に集約**（グローバルナビの「料金」→「ご依頼・ご相談」に変更、2026-08-02）。
  トップの `PricingTeaser` と各ページの料金リンクはすべて `/request#pricing` を指す。

- FAQ は `content.ts` の `faqs[].category`（ai / web / embedded / price / company）で出し分ける。
  各詳細ページは該当カテゴリのみ、`/faq` は全件（FAQPage 構造化データも同じ単位で出力）。
- サービスは `services[].categories` で掲載ページを決める（1つのサービスを複数ページに出せる）。
- ページを増やしたら `sitemap.ts`・`pageLinks`（内部リンク）・`llms.txt` の3つを必ず更新すること。

### ページ内メニューの現在地判定（2026-08-03 に作り直し）

`src/components/fx/useSectionSpy.ts` に集約（`site/PageNav` と `fx/SectionNav` が共用）。

- 判定は**面積比を使わない**。IntersectionObserver の `intersectionRatio` はセクション自身の
  高さに対する比なので、背の低い節ほど有利になり、画面の高さ次第で「1つ前の節」が光っていた
  （例：トップで「事業内容」を押すと「30秒でわかる」に戻る）。
- 現在地＝**アンカーで移動したときに止まる線を、最後に越えたセクション**。
  その線は `html` の `scroll-padding-top` と各セクションの `scroll-margin-top` の**合計**
  （現状は 80px + 80px = 160px）。移動先と判定基準が同じ線になるので、画面サイズで結果が変わらない。
- 線はスタイルの読み込み待ちや画面幅で変わるため、**キャッシュせず毎フレーム測り直す**。
  マウント時に一度だけ測ると、CSS適用前の値を掴んで判定が1つずれる。
- メニューをクリックした直後は、自動スクロールが終わるまで判定を止める（通過する節でのちらつき防止）。

---

## 3.5 ファイル地図（要点）

```
src/lib/site.ts          # ★サイト情報の単一情報源（社名/NAP/geo/エリア/keywords）
src/lib/nav.ts           # ナビ項目（nav/subNav）。★クライアントから読むデータはここに置く（下の鉄則を参照）
src/lib/content.ts       # 掲載コンテンツ（capabilities/services/aiWorkflow/aiImpacts/plans/steps/faqs/keyFacts/aeo）
src/lib/kb.ts            # AIチャットボットの知識源＋BM25検索エンジン（content.tsから自動生成）
src/lib/inquiry.ts       # お問い合わせフォームの選択肢定義（予算・機能など9グループ）
src/lib/jsonld.ts        # 構造化データ（organization/website/webPage/breadcrumb/services/capabilities/howTo/faq）
src/app/layout.tsx       # 全体メタデータ・viewport・共通JSON-LD
src/app/page.tsx         # トップ（各ページの要約と入口だけ。詳細は下層ページへ）
src/app/ai/page.tsx      # /ai AI活用（最優先の導線。AIで作る／AIを作る・AEO/LLMO）
src/app/web/page.tsx     # /web Web制作（AI開発プロセス・サービス・流れ・料金）
src/app/embedded/page.tsx# /embedded 組み込み開発（受託範囲・進め方・お断り範囲）
src/app/faq/page.tsx     # /faq よくある質問（全件をカテゴリ別・FAQPage）
src/app/demo/page.tsx    # /demo できること一覧
src/app/demo/[slug]/     # /demo/<slug> 各デモページ（generateStaticParams で静的生成）
src/app/request/page.tsx # /request ご依頼・ご相談（相談できること・料金・相談の流れ）
src/app/contact/page.tsx # /contact お問い合わせ専用ページ（選択式フォーム）
src/app/company/page.tsx # /company 会社概要（E-E-A-T）
src/app/privacy/page.tsx # /privacy プライバシーポリシー
src/app/actions.ts       # お問い合わせServer Action（★TODO: 送信処理）
src/app/llms.txt/route.ts# /llms.txt 生成（LLMO）
src/app/{sitemap,robots,manifest}.ts
src/app/{opengraph-image,twitter-image,apple-icon}.tsx / icon.svg
src/components/sections/*# 各セクション（Hero/Pillars/DemoShowcase/AiPower/Services/AiSearch/Pricing/PricingTeaser/Process/Faq/RelatedPages...）
                         # Services・Faq・DemoShowcase は props でページごとに出し分ける
src/components/demos/*   # ★実動デモ15種＋共通UI（DemoUi）＋遅延ローダー（DemoLoader）
public/sw.js             # Service Worker（オフライン・キャッシュ・プッシュ受信）
src/components/site/*    # Header/Footer/Logo/Breadcrumbs/PageNav（ページ内メニュー）
src/components/ui/*      # Container/Section/Button/PageHeader/icons
src/components/fx/*      # 3D背景（ThreeBackground/BackgroundFx）・リビール（RevealInit）・カーソル光（CursorGlow）・セクションナビ（SectionNav）
src/components/assistant/*# ★全ページ常駐のAIアシスタント（右下のドット絵）。起動役はkbを読まず、開いたときだけチャット本体を遅延読込
scripts/bundle-size.mjs  # 初期表示の転送量を測る（npm run size）。表示速度の回帰検知用
```

### 表示速度を守るための鉄則（2026-08-02 追加）

見た目が何も変わらないまま重くなるパターンが実際に起きたので、以下を守ること。

- **`"use client"` のコンポーネントから `content.ts` を import しない。**
  1800行ぶんのコンテンツがまるごと初期JSに載る（実測でgzip換算 約17KB）。
  ヘッダーのように全ページ共通かつクライアント側で使うデータは `src/lib/nav.ts` に置く。
- **`next/link` には `prefetch={false}` を付ける。** 付けないと画面内に入っただけで
  リンク先のRSCペイロード（1ページ60〜140KB）を先読みする。トップは本文だけで15本あり、
  スクロールしただけで800KB超を取得していた。ホバー時の先読みはNext.js側に残るため遷移は速いまま。
- **`backdrop-filter`（すりガラス）を、動く背景の上に重ねない。**
  3D背景・オーロラが動いているとぼかし結果をキャッシュできず、スクロール中ずっと再合成が走る。
  ヘッダーはこれが理由で不透明背景に変更済み。`.panel`（1ページに27枚）にはまだ残っている。
- 変更後は `npm run build:pages && npm run size` で数字を確認する。

### 文章を出すときの鉄則（2026-08-02 追加）

- **画面に出す日本語は、JSXに直接書いたものも含めてすべて `ja()` を通す。**
  `{s.description}` ではなく `{ja(s.description)}`、`<strong>制作期間は…</strong>` ではなく
  `<strong>{ja("制作期間は…")}</strong>`。`{siteConfig.name}` のような値の埋め込みも同じ。
  通していないテキストは語の途中で改行される（「従／来」「所／属」「チャット／ボット」）。
  JSXが混ざる見出しなどは `jaNode()`（子要素をたどって中の文字列も処理する）。
  `Button`/`ButtonLink`・`SectionHeading`・`PageHeader` は内部で `jaNode()` 済みなので、
  これらに渡すぶんには不要。JSONLD・llms.txt・metadata には**通さない**（素の文字列のまま使う）。
- **通し漏れの探し方**：ヘッドレスChromeで「日本語を含むのに `span.nb` に包まれていない
  テキストノード」を列挙すると、漏れがそのまま出る。実測が唯一の確認手段。
- **アイコン＋テキストの `li`（flex）では、テキスト側に `min-w-0` を付ける。**
  付けないと折り返し禁止の語がある行でカードが横に膨らむ。
- **`word-break: auto-phrase`（文節での折り返し）は当てにしない（2026-08-03）。**
  `CSS.supports()` は true を返すのに、折り返し位置は何も変わらず、カタカナすら
  「ソフトウェ／ア」と割れることを実測で確認した。**どこで切ってよいかは `ja()`（サーバー側）が決める。**
- **`ja()` は文章を「語のまとまり」に切って `.nb` で包む。** 自立語（漢字・カタカナ・英数字）に
  続くひらがな（送り仮名・助詞）まで1つの塊にするので、「制作期間は」「開発しています」の単位で
  折り返される。ひらがなだけの語も包む（「ください」「いただけます」）。
  上限は全角9文字ぶん（`MAX_WIDTH`）。超えるぶんは次の塊へ送るので、狭い画面でも溢れない。
- **`.nb` を `display: inline-block` にしないこと。** 語を「箱」にすると地の文から切り離され、
  「Webサイト／をつくります」のように助詞だけが次行に落ちる。`white-space: nowrap`（inline のまま）にする。
- **小見出し（h3/h4）に `text-wrap: balance` を使わない（2026-08-03）。** 行の長さを揃えることが
  優先され、「AI機能その／ものを開発している」のように文節を無視した位置で折れる。
  h1/h2 は行数が少なく balance の効果が勝るため、そちらだけ balance のまま。
- 変更後は語中改行と横はみ出しを実測で確認する（ヘッドレスChrome）。

### デモを触るときの鉄則
- **`content.ts` の `demoNote` を必ず維持すること。**「どこまでが実装で、本番では何が変わるか」を
  明示しているブロックで、誇張しないこと自体が E-E-A-T（Trust）の一部になっている。
- デモは必ず `DemoLoader` 経由（`ssr:false`）で読み込む。直接importするとLCPが悪化する。
- `data-reveal` を付けた要素の className を React 側で動的に変えても消えないよう、
  リビール状態は `data-revealed` **属性**で持たせている（class に戻さないこと）。
詳細は `README.md` のディレクトリ構成を参照。

---

## 4. 公開までのTODO（細分化）

進め方：上から順に。`[ ]`→`[x]` で進捗管理。**★＝今の最優先ブロック**。

### Phase 0 ★ 情報確定（ヒアリング反映）
- [ ] `docs/ヒアリングシート.md` の回答を受領
- [x] ブランド名を **EbisuSoft** に確定（2026-07-29）→ 2026-08-02 に表示名を **エビスソフト**（カタカナのみ）へ変更
- [x] 正式名称を **エビスソフト**（法人格なし）に確定・京都商工会所属を明記・事業に**組み込みソフトウェア開発**を追加（2026-08-02）
- [x] 電話（090-8208-7295）・メール（yasufumi2707@icloud.com）・住所（京都市伏見区久我石原町7-37）を反映（2026-07-29）
- [x] 郵便番号を **612-8491** に確定（2026-08-02）
- [x] geo座標をGoogleマップの実測値（34.949037 / 135.724274）に確定（2026-08-02）
- [x] 本番ドメインを **https://ebisusoft.sakura.ne.jp** に確定（2026-08-02）
- [ ] さくらへの初回デプロイ（下記「公開先」を参照）と、完了後の GitHub Pages の noindex 化
- [x] 設立日を **2001-04-01** に確定・営業時間の掲載を廃止（2026-08-02）
- [x] SNSは未開設のため `sameAs` / `twitterHandle` を空に（開設したら追記。空のあいだは構造化データに出力されない）
> `src/lib/site.ts` の `★` はSNSアカウント（未開設）のみ。開設したら `sameAs` / `twitterHandle` に追記する。

### 公開先（2系統）

| 用途 | URL | ビルド | ワークフロー |
| --- | --- | --- | --- |
| **本番** | https://ebisusoft.sakura.ne.jp | `npm run build:sakura`（basePathなし） | `.github/workflows/deploy-sakura.yml`（**手動実行**） |
| プレビュー | https://yasufumi-27.github.io/EbisSoft | `npm run build:pages`（basePath `/EbisSoft`） | `.github/workflows/deploy-pages.yml`（main push で自動） |

さくらへ公開する手順：

1. GitHub の **Settings > Secrets and variables > Actions** に4つ登録する
   （`SAKURA_FTP_HOST` / `SAKURA_FTP_USER` / `SAKURA_FTP_PASSWORD` / `SAKURA_FTP_DIR`）。
   `SAKURA_FTP_DIR` はさくらの公開ディレクトリ（例：`/home/ebisusoft/www`）。
2. Actions タブ > **Deploy to Sakura** > Run workflow で実行。
3. 公開を確認したら、`deploy-sakura.yml` の `push:` トリガーのコメントを外して自動化する。
4. **本番が動いたら、GitHub Pages 側を必ず noindex にする**（同じ内容が2つのURLで公開されると
   検索評価が割れる）。`src/app/robots.ts` で `GITHUB_PAGES=true` のときだけ
   `rules: [{ userAgent: "*", disallow: "/" }]` を返すようにするのが最も簡単。

`out/` には `public/.htaccess` が入る（拡張子なしURLの解決・gzip圧縮・キャッシュ・セキュリティヘッダー）。
静的書き出しでは `next.config.ts` の `headers()` が効かないため、Apache側の設定はここが唯一の置き場所。
- [ ] ロゴ確定：`src/app/icon.svg`・`public/favicon.ico`・`apple-icon.tsx`・OG画像の "E" を正式ロゴへ
- [ ] ブランドカラー確定 → `src/app/globals.css` の `--color-brand` 系

### Phase 1 コンテンツ仕上げ（`src/lib/content.ts`）
- [ ] サービス内容（8種）を実サービスに調整
- [ ] 料金：公開可否を反映（非公開なら料金セクションを「お見積もり」表記へ）
- [ ] **スピードの数値（`aiImpacts` / stats の「最短5日」「約1/3」「Lighthouse 100点」）が
      実態と合っているか確認**。合わない場合は必ず実態に合わせて下げること（誇張はE-E-A-Tで不利）
- [ ] できること（`capabilities`）の文言・使用技術・活用シーンを実態に合わせて確認
- [ ] FAQ：内容精査（AEO/LLMOの観点で“質問→簡潔な答え”を維持）
- [ ] 要点（keyFacts）・強み（strengths）の文言最終化
- [ ] 画像を使う場合：`next/image` で最適化（alt必須）

### Phase 2 必須ページ追加
- [x] プライバシーポリシー（`/privacy`）
- [ ] 特定商取引法の表記（有料サービスを販売する場合）
- [x] 会社概要（`/company`・ローカルSEO/信頼性）
- [x] フッターの `#` プレースホルダリンクを実ページへ接続（`SiteFooter.tsx`）
- [x] 追加ページ分の `sitemap.ts` へのURL追加（全9URL）

### Phase 3 お問い合わせ機能の完成
- [x] 選択式のお問い合わせ専用ページ `/contact` を実装（2026-07-30）
- [ ] `src/app/actions.ts` の `TODO` に**実送信処理**を実装（メール送信：Resend/SendGrid 等、またはフォームサービス）
      ※ 静的配信の現状は mailto 方式。`InquiryForm.tsx` の `buildMailBody()` の戻り値をそのままAPIへPOSTできるよう分離済み
- [ ] 管理者通知＋自動返信メール
- [ ] 送信先・APIキーを環境変数化（`.env.local` / 本番）
- [ ] スパム対策強化が必要なら reCAPTCHA / Cloudflare Turnstile
- [ ] 実送信テスト（成功/失敗/バリデーション）

### Phase 4 SEO / AEO / LLMO 最終調整
- [ ] `.env.local` に本番 `NEXT_PUBLIC_SITE_URL` を設定（canonical/OG/sitemap/llms.txtに反映）
- [ ] 狙うキーワード／AI検索トピック（ヒアリングQ9-5）を見出し・本文・`llms.txt`へ反映
- [ ] Googleビジネスプロフィール（京都市伏見区）を作成し、NAPをサイトと完全一致させる
- [ ] OG画像の最終デザイン（日本語を入れるなら日本語フォント同梱：README参照）
- [ ] GA4 設置／Search Console 確認コード（`NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`）
- [ ] 構造化データを **Rich Results Test** で検証（FAQ/HowTo/LocalBusiness/Service）

### Phase 5 公開前QA
- [ ] 全リンク・全アンカー（#services 等）動作確認
- [ ] **デモ15種の実機確認**（特にiOS Safari：WebGL・スライダー・スクロール連動）
- [ ] レスポンシブ（スマホ/タブレット/PC）確認
- [ ] Lighthouse 計測（Performance/SEO/Accessibility/Best Practices）
- [ ] OGP表示確認（X Card Validator 等）
- [ ] 404ページ確認、誤字脱字・表示崩れチェック

### Phase 6 デプロイ・公開
- [ ] 独自ドメイン取得・DNS設定
- [ ] ホスティング（Vercel想定）へデプロイ設定
- [ ] 本番環境変数を設定（SITE_URL／送信用APIキー／GSC）
- [ ] HTTPS・独自ドメイン接続確認
- [ ] 本番の `robots.txt`／`sitemap.xml`／`llms.txt` のURL確認
- [ ] **Search Console にサイトマップ送信＋インデックス登録**
- [ ] 公開後の全体動作確認

### Phase 7 公開直後（初期運用）
- [ ] アナリティクスでアクセス計測開始を確認
- [ ] インデックス状況を確認
- [ ] AI検索（ChatGPT/Perplexity/AI Overviews）での見え方・引用状況を確認

---

## 5. 検証コマンド（毎回これで確認）

```bash
# 型・Lint（ネット不要）
npx tsc --noEmit
npm run lint

# 本番ビルド（※next/fontのため要ネット → サンドボックス無効で実行）
npm run build

# 表示速度（初期表示の転送量）を数字で確認する
npm run build:pages && npm run size   # ページ別の HTML/JS/CSS/フォント（gzip後）
npm run analyze                       # 何が重いかをモジュール単位で追う（Turbopack公式・localhost:4000）
                                      # ※ npm run analyze は依存追加不要。--output でファイル出力もできる

# 本番サーバーで実機確認
PORT=3100 npm start            # 別プロセスで起動
curl -s localhost:3100/ -o /tmp/home.html      # メタ/JSON-LD等を確認
curl -s localhost:3100/llms.txt                # LLMO
curl -s localhost:3100/robots.txt              # AIクローラー歓迎
curl -s localhost:3100/sitemap.xml
# 終了: lsof -ti tcp:3100 | xargs kill
```

---

## 6. 今後の予定（簡易・公開後）

公開を一旦のゴールとし、その後に着手予定の構想。

- **⏳ さくら本番の Brotli 圧縮（表示速度・未着手）**：
  `public/.htaccess` は現在 `mod_deflate`（gzip）止まり。Brotli にすると HTML/JS/CSS の
  転送量がさらに 15〜20% 減る（トップは gzip 39KB → 30KB前後の見込み）。
  さくらのレンタルサーバで `mod_brotli` が使えるかを先に確認し、使えなければ
  `.br` を事前生成して `mod_rewrite` で配信する方式にする。
  ※ GitHub Pages 側は元から Brotli 対応済みのため、この対応は本番（さくら）向け。
- **`.panel` の `backdrop-filter` 除去（表示速度・未着手）**：
  トップに27枚あり、PCでのスクロール負荷の主因。不透明なグラデーション背景に置き換えると軽くなるが、
  見た目が変わるため要判断。
- **コラム/ブログ機能**（AEO/LLMO強化）：「質問に答える記事」で検索・AI流入を伸ばす。
- **🤖 Web制作エージェント（本命構想）**：
  - 受託のWeb制作業務（ヒアリング→構成設計→コピー→実装→SEO/AEO/LLMO→公開）をAIで遂行するエージェント。
  - 本サイトの構造（`site.ts`/`content.ts`/`jsonld.ts`/セクション部品）を**テンプレート化**し、案件ごとに再利用する想定。
  - `docs/ヒアリングシート.md` を、エージェントの入力フォーマット（要件定義）として転用できる。
  - ＝ このプロジェクト自体が、エージェントの「出力雛形」兼「リファレンス実装」になる。

> 詳細設計は公開達成後に別途検討。まずは Phase 0〜6 を完了させる。

---

## 7. 関連ドキュメント

- `README.md` … セットアップ・構成・実装済みSEO・公開前チェックリスト
- `docs/仕様書.md` … 「〇〇はこうする」の決定事項（✅/🟡/⬜）
- `docs/ヒアリングシート.md` … 顧客への質問票（回答待ち）／将来はエージェントの入力雛形
- `AGENTS.md` … Next.js 16 の注意（コード前にdocsを読む）
