# 引き継ぎ・進捗管理（Handoff & Progress）

> このファイルは **他セッション／他担当（AI含む）への引き継ぎ用** です。
> 作業を始める前に、まずこのファイルと `README.md`・`docs/仕様書.md` を読んでください。
> 最終更新：2026-07-29

---

## 0. このプロジェクトは何か

- **EbisuSoft**（京都市伏見区）の、**Web制作の受注（集客）を目的としたサービスサイト**。
- **一旦のゴール**：このサイトを**公開（本番リリース）**すること。
- **コンセプト**：「**AIを駆使して、最速で、高性能なサイトを**」＋ **SEO / AEO / LLMO（AI検索最適化）に超特化**。
- **差別化の核**：「できること」を主張で終わらせず、**5領域すべてを実際に動くデモとして公開**している
  （3DCG / Webアニメーション / AIチャットボット / SNS連携 / システム連携）。
- **将来構想**：AIで受託業務を遂行する「**Web制作エージェント**」の構築（→ 6章。今は構想のみ）。

---

## 1. 新セッションが最初に知るべきこと（重要・つまずき防止）

1. **これは“いつものNext.js”ではない**：**Next.js 16**（App Router / Turbopack）。APIが学習データと異なる場合がある。
   コードを書く前に `node_modules/next/dist/docs/` の該当ガイドを読む（`AGENTS.md` の指示）。
2. **Bashサンドボックスがネットワークを遮断している**：`npm install` / `npm run build`（`next/font`のフォント取得）/ 外部取得は
   **`dangerouslyDisableSandbox: true` を付けて実行**する。付けないと `ENOTFOUND registry.npmjs.org` で失敗する。
3. **シェルは zsh**：`shopt` 等のbash builtinは使えない。必要なら `bash -c '...'` で実行する。
4. **単一情報源（ここだけ直せば全体に反映）**：
   - `src/lib/site.ts` … 社名・URL・NAP・SNS・keywords・knowsAbout
   - `src/lib/content.ts` … 掲載内容（サービス/料金/実績/お客様の声/FAQ/要点/AEO）
   - `src/lib/jsonld.ts` … 構造化データ（JSON-LD）ビルダー
   - `src/app/globals.css` … ブランドカラー・フォント（`@theme`）
5. **`★` 印 = 差し替え必須のプレースホルダ**（`site.ts` 等）。
6. **検証コマンド**（下記5章）でビルド・型・Lint・実機HTMLを必ず確認する。
7. **技術メモ**：Tailwind **v4**（`@theme`／CSS変数）。`"use server"` ファイルは**async関数しかexport不可**
   （型・定数は別ファイルへ。例：`src/app/contact-state.ts`）。OG画像のsatori既定フォントは**日本語非対応**（画像内はラテン文字主体）。

---

## 2. 現在の進捗（✅ 完了）

**土台＋トップページを、SEO/AEO/LLMO実装込みで構築・ビルド検証済み。**

- ✅ プロジェクト土台：Next.js 16 / React 19 / TS / Tailwind v4
- ✅ トップページ（1枚）：Hero → 要点 → 強み → サービス → AI検索対策 → 実績 → 料金 → 流れ → お客様の声 → FAQ → お問い合わせ
- ✅ 共通：ヘッダー（モバイルメニュー）／フッター（NAP）
- ✅ お問い合わせフォーム：Server Action＋バリデーション＋ハニーポット（**※送信処理は未実装＝TODO**）
- ✅ SEO：メタ／canonical／OGP・Twitter／sitemap／robots／manifest／各アイコン／セキュリティヘッダ／CWV配慮
- ✅ **AEO**：構造化データ（`ProfessionalService`/`WebSite`/`WebPage`/`BreadcrumbList`/`ItemList`/`HowTo`/`FAQPage`）＋`Speakable`＋結論ファースト要点ブロック
- ✅ **LLMO**：`/llms.txt` 設置／AIクローラー（GPTBot・ClaudeBot・PerplexityBot・Google-Extended 等）を明示歓迎／`knowsAbout`
- ✅ ドキュメント：`README.md` / `docs/仕様書.md` / `docs/ヒアリングシート.md`
- ✅ ビルド：12ルート静的生成・`tsc`・`lint` すべてクリーン
- ✅ **デザイン全面リニューアル（2026-07-17）**：近未来×高級感のダークテーマへ刷新
  - テーマ：漆黒ネイビー基調＋シアン（brand）／バイオレット（accent）／シャンパンゴールド（gold）。トークンは `globals.css` の `@theme`
  - **3D背景**：`three` を導入。`src/components/fx/ThreeBackground.tsx`（パーティクル星野＋ワイヤーフレーム多面体＋床グリッド、マウスパララックス、タブ非表示で停止、reduced-motion対応）。`BackgroundFx.tsx` が `next/dynamic`（ssr:false）で遅延読込するため初期HTML/LCPに影響なし
  - **スクロールリビール**：`fx/RevealInit.tsx`＋`[data-reveal]`（CSSはglobals.css）。JS無効時はnoscriptで打ち消し、SEO/AEOへの影響なし
  - UI：コーナーカット＆シーン掃引のボタン（`.btn`系）、ガラスパネル＋ゴールドコーナー（`.panel`系）、発光フォーム（`.field`）、Orbitron（`--font-display`）を英字見出し・数値に使用
  - OG画像・apple-icon・icon.svg・manifestの配色も新テーマへ同期済み
  - 検証済み：`tsc`/`lint`/`build` クリーン、ヘッドレスChromeでPC・モバイル表示とコンソールエラーなしを確認
- ✅ **GitHub Pages公開（2026-07-17）**：https://yasufumi-27.github.io/EbisSoft/
  - `GITHUB_PAGES=true` で `output:"export"`＋`basePath:/EbisSoft`（`next.config.ts`）。mainへのpushで自動デプロイ（`.github/workflows/deploy-pages.yml`）
  - フォームは mailto 方式（静的書き出しではServer Actions不可。実送信実装時は `actions.ts` に戻す）
- ✅ **デザイン強化第2弾＋E-E-A-T＋サービス拡充（2026-07-17）**
  - 演出追加：カードのマウス追従スポットライト（`PointerFx`）、統計カウントアップ（`CountUp`）、ヒーローの3D Tilt（`Tilt`）、スクロール進捗バー（`ScrollProgress`）、テックマーキー（`TechMarquee`）、フィルムノイズ質感、3D背景のスクロールパララックス
  - **E-E-A-T**：代表プロフィール＋品質基準セクション（`Expert.tsx`、`#quality`）を新設。`content.ts` の `expert` / `qualityStandards`（★要差し替え）と `personJsonLd()`（Person構造化データ、organization.founder / webPage.author・reviewedBy から参照）
  - **サービス拡充**：「3DCG制作・WebGL演出」「動画・モーショングラフィックス制作」を services に追加（JSON-LD・llms.txtへ自動反映）。keywords / knowsAbout / FAQ / keyFacts / ヒーロー文言 / メタdescription にも反映

- ✅ **AI訴求へ全面刷新＋実動デモ実装（2026-07-29）**
  - **社名を `EbisSoft` → `EbisuSoft` に変更**、拠点を東京・恵比寿 → **京都市伏見区**へ（`site.ts` / NAP / geo / areaServed / localAreas）
  - **削除**：制作実績（Works）・お客様の声（Testimonials）・「誰が、どんな基準で作るか」（Expert）の3セクション。
    あわせて `personJsonLd` と `aggregateRating`（架空のレビュー評価）も構造化データから撤去
  - **新設セクション**：`AiPower`（AI活用の工程別 AI/人 分担・Before→Afterの短縮実績）、
    `Capabilities`（できること一覧＋その場で動くデモをタブ切替）
  - **実動デモ5種**（`src/components/demos/`）：
    - `Demo3dcg` … Three.js のリアルタイムWebGL（形状/素材/カラー/光量/自動回転/OrbitControls、環境マップで金属・ガラス表現）
    - `DemoAnimation` … CSS + Web Animations API のみで8パターン（外部ライブラリ不使用）
    - `DemoChatbot` … `src/lib/kb.ts` の**日本語N-gram + BM25検索**で根拠つき回答。閾値未満は答えず問い合わせへ誘導
    - `DemoSns` … SNSフィード（サンプルデータ）＋ OGPカードプレビュー（入力に即時追従する実装）
    - `DemoIntegration` … ブラウザ内モックAPI。遅延・失敗・指数バックオフのリトライ・Webhook通知が実際に動く
    - すべて `DemoLoader`（`next/dynamic` ssr:false）で遅延読込 → 初期表示・LCPに影響なし
  - **新規ページ**：`/demo`（一覧）、`/demo/[slug]`（5ページ・静的生成）、`/company`（会社概要）、`/privacy`
  - **SEO/E-E-A-T**：構造化データ刷新（`hasOfferCatalog` / `geo` / `Service`+`WebApplication` / `CollectionPage` / `AboutPage`）、
    表示パンくず（`Breadcrumbs`）、sitemap 9URL、llms.txt をデモ・料金・AI活用まで拡充
  - **バグ修正**：`RevealInit` のリビール状態を class → `data-revealed` 属性へ変更。
    className を動的に切り替える要素が再レンダリングされると React が class を書き戻して**要素が消える**不具合があった
  - **basePath対応の修正**：`manifest.webmanifest` のアイコン／`start_url` と、動的生成ルートである
    `apple-icon` に basePath が自動付与されず404していたため、`NEXT_PUBLIC_BASE_PATH`（`next.config.ts` の `env`）で明示付与するよう修正
  - ⚠️ **既知の挙動（GitHub Pages限定）**：`trailingSlash: false` のため `/demo/` のように
    末尾スラッシュ付きでアクセスすると404になる（`/demo` は200）。サイト内リンク・sitemap・llms.txt は
    すべてスラッシュなしで統一しているため実害はないが、独自ドメイン移行時はホスティング側でリダイレクト設定を推奨
  - 検証済み：`tsc` / `lint` / `build`（通常・`GITHUB_PAGES=true` の両方）クリーン、
    ヘッドレスChromeでデモの実動作30項目・PC/モバイル表示・横はみ出しゼロ・JSエラーゼロを確認

> 連絡先・住所・ドメイン・SNS等は引き続き**仮の値（`★`）**。実情報への差し替えが次の主作業（→ 4章）。

---

## 3. ファイル地図（要点）

```
src/lib/site.ts          # ★サイト情報の単一情報源（社名/NAP/geo/エリア/keywords）
src/lib/content.ts       # 掲載コンテンツ（nav/capabilities/services/aiWorkflow/aiImpacts/plans/steps/faqs/keyFacts/aeo）
src/lib/kb.ts            # AIチャットボットの知識源＋BM25検索エンジン（content.tsから自動生成）
src/lib/jsonld.ts        # 構造化データ（organization/website/webPage/breadcrumb/services/capabilities/howTo/faq）
src/app/layout.tsx       # 全体メタデータ・viewport・共通JSON-LD
src/app/page.tsx         # トップ（セクション組み立て＋ページJSON-LD）
src/app/demo/page.tsx    # /demo できること一覧
src/app/demo/[slug]/     # /demo/<slug> 各デモページ（generateStaticParams で静的生成）
src/app/company/page.tsx # /company 会社概要（E-E-A-T）
src/app/privacy/page.tsx # /privacy プライバシーポリシー
src/app/actions.ts       # お問い合わせServer Action（★TODO: 送信処理）
src/app/llms.txt/route.ts# /llms.txt 生成（LLMO）
src/app/{sitemap,robots,manifest}.ts
src/app/{opengraph-image,twitter-image,apple-icon}.tsx / icon.svg
src/components/sections/*# 各セクション（Hero/AiPower/Capabilities/Services/AiSearch/Pricing/Process/Faq...）
src/components/demos/*   # ★実動デモ5種＋共通UI（DemoUi）＋遅延ローダー（DemoLoader）
src/components/site/*    # Header/Footer/Logo/Breadcrumbs
src/components/ui/*      # Container/Section/Button/PageHeader/icons
src/components/fx/*      # 3D背景（ThreeBackground/BackgroundFx）・スクロールリビール（RevealInit）
```

### デモを触るときの鉄則
- **`content.ts` の `demoNote` を必ず維持すること。**「どこまでが実装で、本番では何が変わるか」を
  明示しているブロックで、誇張しないこと自体が E-E-A-T（Trust）の一部になっている。
- デモは必ず `DemoLoader` 経由（`ssr:false`）で読み込む。直接importするとLCPが悪化する。
- `data-reveal` を付けた要素の className を React 側で動的に変えても消えないよう、
  リビール状態は `data-revealed` **属性**で持たせている（class に戻さないこと）。
詳細は `README.md` のディレクトリ構成を参照。

---

## 4. 公開までのTODO（細分化）

進め方：上から順に。`[ ]`→`[x]` で進捗管理。**★＝今の最優先ブロック**。

### Phase 0 ★ 情報確定（ヒアリング反映）
- [ ] `docs/ヒアリングシート.md` の回答を受領
- [x] ブランド名を **EbisuSoft** に確定（2026-07-29）
- [x] 電話（090-8208-7295）・メール（yasufumi2707@icloud.com）・住所（京都市伏見区久我石原町7-37）を反映（2026-07-29）
- [ ] `src/lib/site.ts` に残る `★` を確定：**郵便番号（612-8493 は要確認）／geo座標（おおよその値）**／営業時間／SNS各URL／設立日／独自ドメイン
- [ ] ロゴ確定：`src/app/icon.svg`・`public/favicon.ico`・`apple-icon.tsx`・OG画像の "E" を正式ロゴへ
- [ ] ブランドカラー確定 → `src/app/globals.css` の `--color-brand` 系

### Phase 1 コンテンツ仕上げ（`src/lib/content.ts`）
- [ ] サービス内容（8種）を実サービスに調整
- [ ] 料金：公開可否を反映（非公開なら料金セクションを「お見積もり」表記へ）
- [ ] **スピードの数値（`aiImpacts` / stats の「最短5日」「約1/3」「Lighthouse 100点」）が
      実態と合っているか確認**。合わない場合は必ず実態に合わせて下げること（誇張はE-E-A-Tで不利）
- [ ] できること（`capabilities`）の文言・使用技術・活用シーンを実態に合わせて確認
- [ ] FAQ：内容精査（AEO/LLMOの観点で“質問→簡潔な答え”を維持）
- [ ] 要点（keyFacts）・強み（strengths）の文言最終化
- [ ] 画像を使う場合：`next/image` で最適化（alt必須）

### Phase 2 必須ページ追加
- [x] プライバシーポリシー（`/privacy`）
- [ ] 特定商取引法の表記（有料サービスを販売する場合）
- [x] 会社概要（`/company`・ローカルSEO/信頼性）
- [x] フッターの `#` プレースホルダリンクを実ページへ接続（`SiteFooter.tsx`）
- [x] 追加ページ分の `sitemap.ts` へのURL追加（全9URL）

### Phase 3 お問い合わせ機能の完成
- [ ] `src/app/actions.ts` の `TODO` に**実送信処理**を実装（メール送信：Resend/SendGrid 等、またはフォームサービス）
- [ ] 管理者通知＋自動返信メール
- [ ] 送信先・APIキーを環境変数化（`.env.local` / 本番）
- [ ] スパム対策強化が必要なら reCAPTCHA / Cloudflare Turnstile
- [ ] 実送信テスト（成功/失敗/バリデーション）

### Phase 4 SEO / AEO / LLMO 最終調整
- [ ] `.env.local` に本番 `NEXT_PUBLIC_SITE_URL` を設定（canonical/OG/sitemap/llms.txtに反映）
- [ ] 狙うキーワード／AI検索トピック（ヒアリングQ9-5）を見出し・本文・`llms.txt`へ反映
- [ ] Googleビジネスプロフィール（京都市伏見区）を作成し、NAPをサイトと完全一致させる
- [ ] OG画像の最終デザイン（日本語を入れるなら日本語フォント同梱：README参照）
- [ ] GA4 設置／Search Console 確認コード（`NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`）
- [ ] 構造化データを **Rich Results Test** で検証（FAQ/HowTo/LocalBusiness/Service）

### Phase 5 公開前QA
- [ ] 全リンク・全アンカー（#services 等）動作確認
- [ ] **デモ5種の実機確認**（特にiOS Safari：WebGL・スライダー・スクロール連動）
- [ ] レスポンシブ（スマホ/タブレット/PC）確認
- [ ] Lighthouse 計測（Performance/SEO/Accessibility/Best Practices）
- [ ] OGP表示確認（X Card Validator 等）
- [ ] 404ページ確認、誤字脱字・表示崩れチェック

### Phase 6 デプロイ・公開
- [ ] 独自ドメイン取得・DNS設定
- [ ] ホスティング（Vercel想定）へデプロイ設定
- [ ] 本番環境変数を設定（SITE_URL／送信用APIキー／GSC）
- [ ] HTTPS・独自ドメイン接続確認
- [ ] 本番の `robots.txt`／`sitemap.xml`／`llms.txt` のURL確認
- [ ] **Search Console にサイトマップ送信＋インデックス登録**
- [ ] 公開後の全体動作確認

### Phase 7 公開直後（初期運用）
- [ ] アナリティクスでアクセス計測開始を確認
- [ ] インデックス状況を確認
- [ ] AI検索（ChatGPT/Perplexity/AI Overviews）での見え方・引用状況を確認

---

## 5. 検証コマンド（毎回これで確認）

```bash
# 型・Lint（ネット不要）
npx tsc --noEmit
npm run lint

# 本番ビルド（※next/fontのため要ネット → サンドボックス無効で実行）
npm run build

# 本番サーバーで実機確認
PORT=3100 npm start            # 別プロセスで起動
curl -s localhost:3100/ -o /tmp/home.html      # メタ/JSON-LD等を確認
curl -s localhost:3100/llms.txt                # LLMO
curl -s localhost:3100/robots.txt              # AIクローラー歓迎
curl -s localhost:3100/sitemap.xml
# 終了: lsof -ti tcp:3100 | xargs kill
```

---

## 6. 今後の予定（簡易・公開後）

公開を一旦のゴールとし、その後に着手予定の構想。

- **コラム/ブログ機能**（AEO/LLMO強化）：「質問に答える記事」で検索・AI流入を伸ばす。
- **🤖 Web制作エージェント（本命構想）**：
  - 受託のWeb制作業務（ヒアリング→構成設計→コピー→実装→SEO/AEO/LLMO→公開）をAIで遂行するエージェント。
  - 本サイトの構造（`site.ts`/`content.ts`/`jsonld.ts`/セクション部品）を**テンプレート化**し、案件ごとに再利用する想定。
  - `docs/ヒアリングシート.md` を、エージェントの入力フォーマット（要件定義）として転用できる。
  - ＝ このプロジェクト自体が、エージェントの「出力雛形」兼「リファレンス実装」になる。

> 詳細設計は公開達成後に別途検討。まずは Phase 0〜6 を完了させる。

---

## 7. 関連ドキュメント

- `README.md` … セットアップ・構成・実装済みSEO・公開前チェックリスト
- `docs/仕様書.md` … 「〇〇はこうする」の決定事項（✅/🟡/⬜）
- `docs/ヒアリングシート.md` … 顧客への質問票（回答待ち）／将来はエージェントの入力雛形
- `AGENTS.md` … Next.js 16 の注意（コード前にdocsを読む）
